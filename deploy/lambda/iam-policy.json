{
  "Comment": "Execution-role policy for the DragnCards on/off Lambda. Replace REGION, ACCOUNT_ID and INSTANCE_ID. DescribeInstances does not support resource-level scoping, hence Resource=*.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "DescribeState",
      "Effect": "Allow",
      "Action": "ec2:DescribeInstances",
      "Resource": "*"
    },
    {
      "Sid": "StartStopThisInstance",
      "Effect": "Allow",
      "Action": ["ec2:StartInstances", "ec2:StopInstances"],
      "Resource": "arn:aws:ec2:REGION:ACCOUNT_ID:instance/INSTANCE_ID"
    }
  ]
}
